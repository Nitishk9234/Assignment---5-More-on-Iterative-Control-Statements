#include <stdio.h>
#include <conio.h>

int main()
{
     int i=1,N;
     printf("Enter a N value :");
     scanf("%d",&N);

     printf("first N odd number \n");
    while (i<=N && i>=1 )
    {
      printf("%d\n",i*2-1);
      i++;
    }
    getch();

}
