#include <stdio.h>
#include <conio.h>

int main()
{
     int i=1,N;
     printf("Enter a N value :");
     scanf("%d",&N);
     printf("print a table of N \n");
    while(i<=10)
    {
      printf("%d\n",i*N);
      i++;
    }
    printf("thank u");

    getch();
}
