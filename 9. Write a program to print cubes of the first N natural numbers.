#include <stdio.h>
#include <conio.h>

int main()
{
     int i=1,N;
     printf("Enter a N value :");
     scanf("%d",&N);
     printf("cube of the first N natural numbers \n");
    while(i<=N)
    {
      printf("%d\n",i*i*i);
      i++;
    }
    printf("thank u");

    getch();
}
