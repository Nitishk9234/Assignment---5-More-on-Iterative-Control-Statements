#include <stdio.h>
#include <conio.h>

int main()
{
     int i=1,N;
     printf("Enter a N value :");
     scanf("%d",&N);
     printf("first N even natural number in reverse order \n");
    do
    {
      printf("%d\n",N*2);
      N--;
    }
    while(i<=N);
    printf("end");

    getch();
}
