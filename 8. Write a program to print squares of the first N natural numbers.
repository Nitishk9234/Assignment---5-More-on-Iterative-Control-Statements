#include <stdio.h>
#include <conio.h>

int main()
{
     int i=1,N;
     printf("Enter a N value :");
     scanf("%d",&N);
     printf("squares of the first N natural numbers \n");
    while(i<=N)
    {
      printf("%d\n",i*i);
      i++;
    }
    printf("end");
    getch();
}
